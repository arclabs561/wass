# Managed by repository-settings/app
# https://github.com/repository-settings/app

repository:
  description: "Optimal transport primitives: Wasserstein distance, Sinkhorn algorithm, and Sinkhorn divergences (balanced + unbalanced)."
  homepage: "https://docs.rs/wass"
  private: false
  has_issues: true
  has_wiki: false
  has_downloads: true
  default_branch: main
  allow_squash_merge: true
  allow_merge_commit: true
  allow_rebase_merge: true
  delete_branch_on_merge: true

  topics:
    - distribution-comparison
    - entropic-regularization
    - optimal-transport
    - rust
    - sinkhorn-algorithm
    - sinkhorn-divergence
    - unbalanced-optimal-transport
    - wasserstein-distance
